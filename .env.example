# Local Development Environment Variables
#
# NOTE: For docker-compose users, this file is OPTIONAL.
# Docker Compose uses these same defaults automatically if no .env file exists.
#
# Only copy this if you want to customize settings:
#   cp .env.example .env
#
# For local Go development (without Docker), you MUST create .env:
#   cp .env.example .env

# Database Configuration (for local Docker PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=vehicles_db
DB_SSLMODE=disable

# API Configuration
API_PORT=8080
GIN_MODE=debug
