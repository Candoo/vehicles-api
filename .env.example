# Local Development Environment Variables
# Copy this file to .env to get started: cp .env.example .env

# Database Configuration (for local Docker PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=vehicles_db
DB_SSLMODE=disable

# API Configuration
API_PORT=8080
GIN_MODE=debug
