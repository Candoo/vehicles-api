# Production Environment Variables
# Copy this to .env.production and fill in your actual values

# External Database Configuration (e.g., AWS RDS, Cloud SQL, etc.)
DB_HOST=your-production-db-host.amazonaws.com
DB_PORT=5432
DB_USER=prod_vehicles_user
DB_PASSWORD=your_very_strong_password_here
DB_NAME=vehicles_production
DB_SSLMODE=require

# API Configuration
API_PORT=8080
GIN_MODE=release

# Usage:
# docker-compose -f docker-compose.yml -f docker-compose.prod.yml --env-file .env.production up -d
